apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beansnet-sites.fullname" . }}-test-connection"
  labels:
{{ include "beansnet-sites.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "beansnet-sites.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
