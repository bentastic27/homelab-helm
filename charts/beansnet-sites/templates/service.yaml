apiVersion: v1
kind: Service
metadata:
  name: {{ include "beansnet-sites.fullname" . }}
  labels:
{{ include "beansnet-sites.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "beansnet-sites.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
